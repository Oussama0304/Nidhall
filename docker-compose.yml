version: '3.8'

services:
  database:
    image: mysql:8.0
    container_name: agil_pip_database
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=ProjectPfeAgil
      - MYSQL_DATABASE=ProjetPfeAgil
    ports:
      - "3306:3306"
    volumes:
      - ./database.sql:/docker-entrypoint-initdb.d/database.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  backend:
    build: .
    container_name: agil_pip_backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DB_HOST=database
      - DB_USER=root
      - DB_PASSWORD=ProjectPfeAgil
      - DB_NAME=ProjetPfeAgil
      - EMAIL_USER=nidhal.boughanmi@esprit.tn
      - EMAIL_PASS=Nidhalboughanmi123
      - HUGGINGFACE_API_KEY=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
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./public:/app/public
    depends_on:
      database:
        condition: service_healthy
    networks:
      - app-network

  frontend:
    build: ./pfe-project-front
    container_name: agil_pip_frontend
    ports:
      - "8095:80"
    environment:
      - REACT_APP_API_URL=http://localhost:3000
      - REACT_APP_UPLOADS_URL=http://localhost:3000/uploads
      - REACT_APP_PUBLIC_URL=http://localhost:3000/public
      - NODE_ENV=development
      - PORT=8095
    volumes:
      - ./pfe-project-front:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  uploads:
  public: